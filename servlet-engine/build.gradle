plugins {
    id "io.micronaut.build.internal.servlet-module"
}

dependencies {
    annotationProcessor libs.micronaut.graal

    api project(":servlet-core")
    api libs.managed.servlet.api

    implementation libs.projectreactor

    testAnnotationProcessor libs.micronaut.inject.java
    testImplementation libs.google.cloud.functions
}
